{
  "eli3": [
    "The chain rule tells you how to differentiate composed functions. If y = f(g(x)), the rate of change of y with respect to x is the product: dy/dx = f'(g(x)) · g'(x). Think of it as rates multiplying along a chain.",
    "Imagine a production line: machine A processes input x into intermediate g(x), then machine B processes that into final output f(g(x)). The chain rule says: total change = (B's sensitivity to input) × (A's sensitivity to input).",
    "In multiple dimensions, the chain rule uses matrix multiplication (Jacobians). If h(x) = f(g(x)), then Jh = Jf · Jg. This is why backpropagation works — gradients flow backward through the network via successive matrix multiplications."
  ],
  "whenWhy": [
    {
      "title": "Backpropagation in neural networks",
      "body": "Neural networks are compositions of layers: fₙ∘fₙ₋₁∘…∘f₁(x). Backprop applies chain rule backward from loss to compute ∂L/∂W for all weights. Each layer passes gradients to previous layer via Jacobian-vector products."
    },
    {
      "title": "Change of variables in probability",
      "body": "For random variable transformation Y = g(X), the PDF transforms as f_Y(y) = f_X(g⁻¹(y)) · |d/dy g⁻¹(y)|. The determinant of the Jacobian accounts for how g stretches/compresses space."
    },
    {
      "title": "Differentiating complex loss functions",
      "body": "Loss functions often compose multiple operations: L = loss(model(parameters(data))). Chain rule decomposes this into manageable pieces: ∂L/∂params = ∂L/∂output × ∂output/∂params."
    },
    {
      "title": "ODEs and dynamical systems",
      "body": "If y depends on x, and x depends on t, then dy/dt = (dy/dx)(dx/dt). Used extensively in physics (related rates), control theory, and when analyzing optimization dynamics (gradient flow)."
    }
  ],
  "deepDiveIntro": [
    "For a composed function y = f(g(x)), the chain rule says the overall rate of change is the product of the individual rates: how fast f changes with its input, multiplied by how fast g changes with x. In Leibniz notation this looks like fraction cancellation: the intermediate quantity dg appears in both numerator and denominator. The rule extends to multiple variables by summing contributions from each input path.",
    "For vector-valued functions, the chain rule becomes matrix multiplication of Jacobians. When h is the composition of two functions, the matrix of partial derivatives of h equals the matrix of partial derivatives of the outer function times the matrix of partial derivatives of the inner function. This is the mathematical engine behind backpropagation in neural networks."
  ],
  "visualizationType": "chain-rule",
  "deepDive": [
    {
      "heading": "Single Variable Chain Rule",
      "body": "When one function feeds into another, the total rate of change comes from multiplying the two local sensitivities. First you measure how the outer function reacts to its input, then you multiply by how the inner function changes that input. This simple idea is why composite expressions differentiate so naturally.",
      "formula": "(f∘g)'(x) = f'(g(x)) · g'(x)"
    },
    {
      "heading": "Multivariable Chain Rule",
      "body": "If an output depends on several intermediate quantities, the total change comes from adding the contribution from every dependency path. Each path contributes a local sensitivity multiplied by the rate at which the intermediate quantity changes. This is the bookkeeping rule behind related-rates problems and many multivariable derivatives.",
      "formula": "dz/dt = Σᵢ (∂f/∂xᵢ)(dxᵢ/dt)"
    },
    {
      "heading": "Chain Rule for Jacobians",
      "body": "For vector-valued functions, composing maps means their local linear approximations compose as well. That is why the Jacobian of a composition is the product of the individual Jacobians in the correct order. This matrix view is the mathematical core of backpropagation.",
      "formula": "Jh = Jf · Jg"
    },
    {
      "heading": "Backpropagation as Chain Rule",
      "body": "Backpropagation applies the chain rule across many layers while avoiding the cost of building huge Jacobian matrices explicitly. It repeatedly combines the incoming gradient with local derivatives as it moves backward through the computation graph. That makes gradient computation practical even for very large neural networks."
    },
    {
      "heading": "Higher-Order Chain Rule (Faà di Bruno)",
      "body": "For second and higher derivatives, the chain rule becomes more intricate because curvature terms from many interactions must be combined. These formulas are rarely written out by hand in machine learning, but they matter for second-order methods and theoretical error analysis."
    }
  ],
  "youtubeLabel": "Chain Rule — 3Blue1Brown",
  "youtubeUrl": "https://www.youtube.com/embed/YG15m2VwSjA",
  "takeaways": [
    "Chain rule: (f∘g)'(x) = f'(g(x)) · g'(x).",
    "Multivariable: dz/dt = Σ (∂f/∂xᵢ)(dxᵢ/dt). Sum over all paths.",
    "Vector version: Jh = Jf · Jg (matrix multiplication of Jacobians).",
    "Backpropagation is efficient application of chain rule via VJPs.",
    "Leibniz notation makes chain rule look like fraction cancellation."
  ],
  "edgeCases": [
    {
      "q": "What if g is not differentiable at some point?",
      "a": "The chain rule requires differentiability. If g is not differentiable at x but f is constant near g(x), f∘g might still be differentiable. For non-differentiable g (like ReLU at 0), we use subgradients: ∂(f∘g)(x) contains products of subgradients."
    },
    {
      "q": "How does chain rule work with higher-order derivatives?",
      "a": "Apply product rule to chain rule result. For (f∘g)'': differentiate f'(g(x))g'(x) using both product and chain rules: (f∘g)'' = f''(g)(g')² + f'(g)g''. This gets complicated quickly (Faà di Bruno formula)."
    },
    {
      "q": "Why is backpropagation more efficient than naive chain rule?",
      "a": "Naive evaluation would compute Jacobians explicitly (large matrices). Backprop uses vector-Jacobian products (VJPs), computing vᵀJ without forming J. This reduces complexity from O(nodes²) to O(nodes) for typical network architectures."
    }
  ],
  "sources": [
    { "label": "Stewart — Calculus: Early Transcendentals (8th ed.), Ch. 3", "url": "https://www.cengage.com/c/calculus-early-transcendentals-8e-stewart/" },
    { "label": "Baydin et al. — Automatic Differentiation in Machine Learning", "url": "https://arxiv.org/abs/1502.05767" }
  ]
}
