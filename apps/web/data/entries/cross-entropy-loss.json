{
  "visualizationType": "cross-entropy",
  "codeSnapshot": {
    "label": "cross_entropy_loss.py",
    "code": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n# Multi-class cross-entropy (PyTorch)\n# Input: raw logits (no softmax applied)\ncriterion = nn.CrossEntropyLoss()\nlogits = torch.tensor([[2.0, 1.0, 0.1],   # sample 1\n                       [0.5, 2.5, 0.3]])   # sample 2\nlabels = torch.tensor([0, 1])              # true classes\nloss = criterion(logits, labels)\nprint(f\"CE Loss: {loss.item():.4f}\")  # ~0.4971\n\n# Manual: -log(softmax(logit)[true_class])\nprobs = F.softmax(logits, dim=1)\nce_manual = -torch.log(probs[torch.arange(2), labels]).mean()\nprint(f\"Manual CE: {ce_manual.item():.4f}\")  # same\n\n# Binary cross-entropy\nbce = nn.BCEWithLogitsLoss()\ny_logit = torch.tensor([1.5, -0.5, 2.0])\ny_true = torch.tensor([1.0, 0.0, 1.0])\nprint(f\"BCE Loss: {bce(y_logit, y_true).item():.4f}\")\n\n# Label smoothing built in to CrossEntropyLoss\nsmoothed = nn.CrossEntropyLoss(label_smoothing=0.1)\nprint(f\"Smoothed CE: {smoothed(logits, labels).item():.4f}\")"
  },
  "eli3": [
    "Cross-entropy loss asks: 'How surprised is my model when it sees the true answer?' If the model said class A had 90% probability and class A was indeed correct, it's not surprised — low loss. If it gave class A 1% probability and class A was correct, it's very surprised — high loss.",
    "Mathematically it's −log(predicted probability of the true class). Log makes surprises compound: being 10× less confident costs you not twice but log(10) ≈ 2.3 times more loss.",
    "It's the standard loss for classification because it perfectly aligns with maximum likelihood estimation and produces strong, non-vanishing gradients — unlike MSE on probabilities, which causes the model to learn slowly when predictions are very wrong."
  ],
  "whenWhy": [
    {
      "title": "Multi-class classification (softmax output)",
      "body": "For K classes, apply softmax to logits to get probabilities p̂, then L = −log p̂_{true_class}. Cross-entropy with softmax is numerically stable when computed jointly (log-sum-exp trick). PyTorch's nn.CrossEntropyLoss takes raw logits and handles this internally. Every image classifier (ResNet, ViT, EfficientNet) uses this loss."
    },
    {
      "title": "Binary classification (sigmoid output)",
      "body": "Binary CE: L = −[y log(p) + (1−y) log(1−p)]. Use BCEWithLogitsLoss (applies sigmoid + CE in one numerically stable step). Used in multi-label classification where each label is independent — not softmax but independent sigmoid per label."
    },
    {
      "title": "Language model training (next-token prediction)",
      "body": "GPT and other causal LMs minimise the average cross-entropy over all token positions: L = −(1/T) Σₜ log p_θ(wₜ|w₁,...,wₜ₋₁). This is the standard pre-training objective for all autoregressive LLMs. Perplexity = exp(CE) is the standard reporting metric."
    }
  ],
  "deepDiveIntro": [
    "For a multi-class problem, cross-entropy is simply the negative log probability assigned to the correct class. Because only the true class contributes (all other terms in the sum are zero), the loss is −log(predicted probability of the true class). The gradient of the combined softmax plus cross-entropy with respect to the raw logits has an elegant closed form: it is just the predicted probability minus the one-hot target — a clean, non-vanishing signal even when the model is confidently wrong. [1]",
    "Minimising cross-entropy is equivalent to minimising the KL divergence between the target distribution and the model's predictions, and also equivalent to maximising the log-likelihood of the data under a categorical model. These three perspectives all point to the same objective, which is why cross-entropy is the canonical loss for classification. [1][2]"
  ],
  "deepDive": [
    {
      "heading": "Gradient of CE + Softmax",
      "body": "The gradient of cross-entropy with softmax outputs has a beautifully clean form: it equals the model's predicted probability minus the one-hot target label. For the correct class, the gradient is negative (loss decreases when you increase that logit), and for all other classes it is positive. This gradient is never zero even when the model is confidently wrong, which is why cross-entropy trains much faster than MSE on classification problems where a sigmoid saturates with vanishing gradient. [1]",
      "formula": "∂L/∂zⱼ = p̂ⱼ − 1{j = true class}"
    },
    {
      "heading": "Numerical Stability: Log-Sum-Exp Trick",
      "body": "Computing the softmax of large logits naively leads to overflow because the exponentials get astronomically large. The fix is to subtract the maximum logit before exponentiating — the result is mathematically identical but stays within floating-point range. PyTorch's cross-entropy function and LogSoftmax implement this automatically. Never manually apply softmax and then take the log separately — precision loss compounds and you lose digits. [1]"
    },
    {
      "heading": "Label Smoothing",
      "body": "With hard one-hot targets, the model is pushed to assign all the probability to the correct class, which leads to overconfident predictions that generalise poorly. Label smoothing softens the target by spreading a small fraction (typically 0.1) of probability mass evenly across all other classes. This regularises the model, reduces calibration error, and was used in the original transformer paper and in training EfficientNet. [2]",
      "formula": "ỹₖ = 1−ε if k=true, ε/(K−1) otherwise"
    },
    {
      "heading": "Focal Loss: Addressing Class Imbalance",
      "body": "Focal loss multiplies the standard cross-entropy by a factor that shrinks toward zero when the model is already confident about an example. This down-weights the contribution of easy examples — where the model correctly assigns high probability to the true class — so the training signal is dominated by the hard cases the model is getting wrong. With a typical exponent of 2, the contribution from easy negatives is reduced by roughly 100x compared to standard cross-entropy. This was introduced for object detection where the vast majority of background patches are easy negatives. [3]",
      "formula": "FL = −(1 − p̂_c)^γ log p̂_c"
    }
  ],
  "youtubeLabel": "Cross Entropy Loss — StatQuest",
  "youtubeUrl": "https://www.youtube.com/embed/6ArSyxRG63I",
  "takeaways": [
    "L = −log p̂_{true class} — penalises low probability assigned to the correct class. [1]",
    "Gradient ∂L/∂zⱼ = p̂ⱼ − 1{j=true}: clean, linear, non-vanishing. [1]",
    "CE = KL(y||p̂) for one-hot labels — minimising CE ≡ maximising log-likelihood. [1]",
    "Label smoothing (ε=0.1) reduces overconfidence and improves calibration. [2]",
    "Focal loss = CE weighted by (1−p̂)^γ — targets hard examples in imbalanced datasets. [3]"
  ],
  "edgeCases": [
    {
      "q": "Why does CE produce better gradients than MSE for classification?",
      "a": "With MSE and sigmoid: ∂L/∂z = (ŷ−y)·σ'(z) = (ŷ−y)·ŷ(1−ŷ). When a confident wrong prediction saturates the sigmoid (ŷ≈0 or 1), σ'(z)≈0 and the gradient vanishes — learning stalls. With CE and softmax: ∂L/∂z = ŷ−y, no saturation term. Even if the model is confidently wrong (e.g., class 0 gets 99% but true class is 1), the gradient is large, enabling rapid correction."
    },
    {
      "q": "What is temperature scaling in the context of cross-entropy?",
      "a": "Temperature T divides logits before softmax: p̂ₖ = exp(zₖ/T)/Σⱼ exp(zⱼ/T). High T (>1) makes the distribution more uniform (lower confidence); low T (<1) makes it more peaked (higher confidence). In knowledge distillation, teacher's soft labels at high T carry more information about similarities between classes than hard one-hot labels. In inference, temperature controls exploration vs exploitation (used in LLM sampling). Temperature does not change the argmax at T=1 but affects cross-entropy loss value."
    }
  ],
  "sources": [
    { "label": "Goodfellow, Bengio & Courville — Deep Learning, Ch. 6", "url": "https://www.deeplearningbook.org/" },
    { "label": "Vaswani et al. (2017) — Attention Is All You Need arXiv:1706.03762", "url": "https://arxiv.org/abs/1706.03762" },
    { "label": "Lin et al. (2017) — Focal Loss for Dense Object Detection arXiv:1708.02002", "url": "https://arxiv.org/abs/1708.02002" }
  ]
}
