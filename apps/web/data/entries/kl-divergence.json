{
  "eli3": [
    "KL divergence measures how different two probability distributions are. It's not symmetric: KL(p||q) ≠ KL(q||p). Think of it as the 'cost' of using distribution q to approximate the true distribution p.",
    "Formula: KL(p||q) = Σ p(x) log(p(x)/q(x)). If q assigns low probability where p is high, KL is large (bad approximation). If q matches p exactly, KL = 0.",
    "Unlike a distance metric, KL divergence isn't symmetric and doesn't satisfy the triangle inequality. But it's always non-negative (Gibbs' inequality), making it useful for optimization."
  ],
  "whenWhy": [
    {
      "title": "Variational inference",
      "body": "Approximate complex posterior p(θ|D) with simpler q(θ) by minimizing KL(q||p). Evidence lower bound (ELBO) = log p(D) - KL(q||p). Used in VAEs, Bayesian neural networks, and topic models."
    },
    {
      "title": "Reinforcement learning (policy optimization)",
      "body": "TRPO and PPO constrain policy updates using KL divergence. Prevent overly large steps that destabilize training. Natural gradient descent uses KL to define trust regions in parameter space."
    },
    {
      "title": "Model comparison and goodness-of-fit",
      "body": "Expected log-likelihood relates to KL between true and estimated distributions. AIC and BIC penalize model complexity. Minimum KL estimation is asymptotically equivalent to maximum likelihood."
    },
    {
      "title": "Domain adaptation and transfer learning",
      "body": "Minimize KL between source and target domain distributions. Align feature distributions for better transfer. Used in domain adversarial neural networks (DANN) and importance weighting."
    }
  ],
  "deepDiveIntro": [
    "For discrete distributions p and q on the same support, KL divergence (relative entropy) is D(p||q) = Σ_x p(x) log(p(x)/q(x)) = E_p[log(p/q)]. Convention: 0 log 0 = 0, and if p(x) > 0 while q(x) = 0, then D(p||q) = ∞. Continuous version: D(p||q) = ∫ p(x) log(p(x)/q(x)) dx.",
    "Gibbs' inequality: D(p||q) ≥ 0 with equality iff p = q almost everywhere. Not symmetric: D(p||q) ≠ D(q||p). Not a metric (no triangle inequality). Forward KL (mean-seeking): D(p||q) forces q to cover all of p. Reverse KL (mode-seeking): D(q||p) allows q to focus on one mode of p."
  ],
  "visualizationType": "kl-divergence",
  "deepDive": [
    {
      "heading": "Definition and Properties",
      "body": "KL divergence measures how much information is lost when one distribution is used to approximate another. It is zero only when the two distributions agree, and it grows as the mismatch becomes more severe. Because swapping the two arguments changes the meaning, KL divergence should be read as a directed comparison rather than a geometric distance.",
      "formula": "D(p||q) ≥ 0, equality iff p = q"
    },
    {
      "heading": "Forward vs Reverse KL",
      "body": "The two directions of KL divergence behave differently. Forward KL strongly penalizes missing regions where the true distribution has probability mass, so it tends to spread the approximation out to cover everything important. Reverse KL is more willing to focus on a few high-probability regions, which is why it often behaves like a mode-seeking objective in variational inference.",
      "formula": "Forward (moment projection): D(p||q). Reverse (information projection): D(q||p)"
    },
    {
      "heading": "Connection to Maximum Likelihood",
      "body": "Maximum likelihood can be understood as choosing model parameters so the model distribution gets as close as possible to the true data-generating distribution in the KL sense. When you average log-likelihood over data, you are rewarding models that place high probability on what actually occurs. That is why likelihood maximization and KL minimization point to the same fitted model under the usual setup.",
      "formula": "argmin_θ D(p||q_θ) = argmax_θ E_p[log q_θ(X)]"
    },
    {
      "heading": "Variational Inference",
      "body": "Variational inference uses KL divergence to replace an intractable posterior with a simpler approximation. Instead of matching the posterior directly, it optimizes a lower bound that balances data fit against staying close to the prior. This turns Bayesian inference into an optimization problem that is practical for large models.",
      "formula": "ELBO = E_q[log p(x,z)] - E_q[log q(z)] = log p(x) - KL(q||p)"
    },
    {
      "heading": "Information Projection and I-Projection",
      "body": "Projection language highlights that KL divergence depends on which distribution is treated as the target and which one must live inside a restricted family. One direction tends to preserve overall average behavior, while the other tends to lock onto the most probable regions. This distinction is important when choosing approximation methods for probabilistic models.",
      "formula": "I-projection: min D(p||q),   M-projection: min D(q||p)"
    }
  ],
  "youtubeLabel": "KL Divergence — Machine Learning Street Talk",
  "youtubeUrl": "https://www.youtube.com/embed/ErfnhcEV9O8",
  "takeaways": [
    "KL(p||q) = Σ p(x) log(p(x)/q(x)): cost of using q instead of p.",
    "Always non-negative, zero iff p = q. Not symmetric.",
    "Forward KL (D(p||q)): mean-seeking, covers all modes.",
    "Reverse KL (D(q||p)): mode-seeking, can ignore modes.",
    "MLE minimizes KL between true distribution and model."
  ],
  "edgeCases": [
    {
      "q": "What if q(x) = 0 where p(x) > 0?",
      "a": "Then D(p||q) = ∞. This is a key property: forward KL requires q to have support covering p's support. In variational inference, this means q must be flexible enough to place mass where the posterior has mass, or the objective blows up."
    },
    {
      "q": "Why is KL divergence not a metric?",
      "a": "(1) Not symmetric: D(p||q) ≠ D(q||p) in general. (2) No triangle inequality. It satisfies non-negativity and identity (D=0 iff p=q), but fails other metric properties. This makes it an 'information divergence' rather than a true distance."
    },
    {
      "q": "What's the relationship between KL and cross-entropy?",
      "a": "H(p,q) = H(p) + D(p||q). Cross-entropy = entropy + KL divergence. Since H(p) is constant (true distribution), minimizing cross-entropy is equivalent to minimizing KL divergence. This is why cross-entropy loss in classification is really minimizing KL between true labels and predictions."
    }
  ],
  "sources": [
    { "label": "Cover & Thomas — Elements of Information Theory (2nd ed.), Ch. 2", "url": "https://www.wiley.com/en-us/Elements+of+Information+Theory" },
    { "label": "Murphy — Machine Learning: A Probabilistic Perspective, Ch. 21", "url": "https://mitpress.mit.edu/books/machine-learning-0" }
  ]
}
