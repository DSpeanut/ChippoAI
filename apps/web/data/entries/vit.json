{
  "visualizationType": "vit-viz",
  "codeSnapshot": {
    "label": "vit_demo.py",
    "code": "import torch\nimport timm  # pip install timm\n\n# --- Load pretrained ViT-B/16 ---\nmodel = timm.create_model('vit_base_patch16_224', pretrained=True)\nmodel.eval()\n\nparams = sum(p.numel() for p in model.parameters())\nprint(f\"ViT-B/16 parameters: {params/1e6:.1f}M\")  # ~86M\n\n# --- Inspect patch embedding ---\nprint(f\"Patch embed: {model.patch_embed}\")\n# PatchEmbed: kernel_size=16, stride=16, in_chans=3, embed_dim=768\n# Number of patches: (224/16)^2 = 196\n\n# --- Forward pass ---\nx = torch.randn(1, 3, 224, 224)  # [B, C, H, W]\nwith torch.no_grad():\n    features = model.forward_features(x)  # [1, 197, 768]  (196 patches + 1 CLS)\n    logits   = model(x)                   # [1, 1000]\n\nprint(f\"Feature shape (incl CLS): {features.shape}\")  # [1, 197, 768]\nprint(f\"Output shape:            {logits.shape}\")     # [1, 1000]\n\n# --- CLS token vs patch tokens ---\ncls_token    = features[:, 0, :]     # [1, 768]  — used for classification\npatch_tokens = features[:, 1:, :]    # [1, 196, 768]  — spatial features\nprint(f\"CLS token shape:   {cls_token.shape}\")\nprint(f\"Patch token shape: {patch_tokens.shape}\")"
  },
  "eli3": [
    "Before ViT, everyone assumed convolutional networks were the only way to process images. ViT challenged this by treating an image as a sequence of patches — just like how BERT treats a sentence as a sequence of words. Cut the image into 16×16 tiles, flatten each tile into a vector, and feed the sequence to a standard Transformer.",
    "A special [CLS] token is prepended to the sequence (borrowed from BERT). After the Transformer encoder processes all patches (allowing each patch to attend to all others), the CLS token's final representation is used for classification — it acts as a summary of the whole image.",
    "The catch: ViT needs much more data than CNNs to train from scratch. CNNs have translation equivariance baked in (the same filter slides over the whole image), but ViT has no such prior — it must learn spatial relationships from scratch, requiring large datasets (ImageNet-21k or JFT-300M). But once pretrained on enough data, ViT matches or beats CNNs on most vision tasks."
  ],
  "whenWhy": [
    {
      "title": "Large-scale image classification with large datasets",
      "body": "ViT matches CNN performance on ImageNet (1.2M images) when pretrained on larger datasets (ImageNet-21k: 14M images, or JFT-300M: 300M images). At scale (ViT-L/16 pretrained on JFT-300M), ViT achieves 88.5% top-1 on ImageNet — higher than EfficientNet-B7. The inductive biases of CNNs (local connectivity, weight sharing) are helpful for small datasets but become less important at scale when the model can learn these patterns from data. ViT is now the preferred architecture for large-scale vision pretraining."
    },
    {
      "title": "Multimodal models (CLIP, GPT-4V, LLaVA)",
      "body": "ViT is the standard visual encoder in multimodal models. CLIP trains a ViT alongside a text Transformer using contrastive loss on 400M image-text pairs. GPT-4V, LLaVA, and Flamingo all use ViT as the image encoder. The advantage over CNNs: Transformers share the same architecture across modalities, simplifying cross-attention between visual and text tokens. The ViT's patch tokens (one per image patch) can be directly attended to by a language model decoder, enabling efficient visual question answering."
    },
    {
      "title": "Dense prediction tasks with DINO and masked image modeling",
      "body": "ViT features learned via self-supervised objectives (DINO, DINOv2, MAE) provide excellent dense prediction representations. DINO ViT-S features naturally localise objects without any segmentation supervision — each patch token's representation reflects its semantic content. DINOv2 ViT-G features are the current state of the art for linear probing on depth estimation, segmentation, and retrieval without any fine-tuning. MAE (Masked Autoencoder, He et al., 2022) masks 75% of patches and trains ViT to reconstruct them — efficient pretraining that requires only a fraction of the patches for the encoder."
    }
  ],
  "deepDiveIntro": [
    "Dosovitskiy et al. (2020) applied a standard Transformer encoder to images with minimal modifications. Image x ∈ ℝ^{H×W×C} is split into N = HW/P² non-overlapping patches of size P×P, where P=16 is typical. Each patch is flattened to ℝ^{P²·C} and linearly projected to ℝ^D (patch embedding): z_i = E·x_i + e^{pos}_i, where E ∈ ℝ^{D×P²C} is the patch embedding matrix and e^{pos}_i is a learned positional embedding. A learnable [CLS] token z_0 = e^{cls} is prepended. The resulting sequence of N+1 tokens is processed by L Transformer encoder layers. The [CLS] token's final representation is passed to a classification head: y = LayerNorm(z^L_0)W_{head}. For ViT-B/16: P=16, D=768, L=12, n_heads=12, N=196 patches for 224×224 input. [1]",
    "ViT's lack of CNN inductive biases (local receptive fields, weight sharing, translation equivariance) means it must learn these properties from data. For ViT-B/16 trained on ImageNet-1k (1.2M images) from scratch, top-1 accuracy is ~77% — lower than ResNet-50 (~76%) despite 3× more parameters, and much lower than training with JFT-300M. The data efficiency problem is addressed by: (1) data augmentation (RandAugment, Mixup, CutMix); (2) pretraining on larger datasets then fine-tuning; (3) self-supervised pretraining (DINO, MAE); (4) hybrid ViT (CNN stem for early feature extraction, Transformer for later processing). DeiT (Touvron et al., 2021) showed that strong data augmentation + distillation from a CNN teacher enables ViT to match CNNs on ImageNet with only 1.2M training images. [2]"
  ],
  "deepDive": [
    {
      "heading": "Patch Embedding and Input Preparation",
      "body": "Using Conv2d is computationally efficient — PyTorch optimises this as a single convolution. Each position gets a learned positional embedding added."
    },
    {
      "heading": "ViT vs CNN: Inductive Biases and Data Efficiency",
      "body": "CNNs have two strong inductive biases: (1) locality — each output depends on a local receptive field; (2) translation equivariance — shifting the input shifts feature maps. These biases encode the prior that 'images have local, translation-invariant structure' — a prior that holds for natural images and makes CNNs very sample-efficient on visual data. ViT has neither bias by design — any patch can attend to any other patch from the first layer. Empirically, this hurts on small datasets (ViT-B needs ~10× more training images to match ResNet-50) but is beneficial at scale (ViT can model arbitrary long-range dependencies without architectural constraints). Dosovitskiy et al. visualised positional embedding similarity: nearby patches have more similar embeddings, suggesting ViT learns spatial structure implicitly but requires more data to do so. [2]"
    },
    {
      "heading": "Self-Supervised ViT: DINO and MAE",
      "body": "DINO (Caron et al., 2021) trains ViT via self-distillation: a student network learns to match the output of an exponential moving average teacher network on different views (random crops) of the same image, without labels. Key finding: DINO ViT features contain remarkable spatial structure without any supervision — the attention maps from the last layer segment foreground objects accurately. This emergent segmentation motivated widespread use of ViT in dense prediction. MAE (He et al., 2022) masks 75% of patches at random and trains ViT to reconstruct pixel values of masked patches using only visible patches as input. Masking 75% is surprisingly effective because adjacent patches have high redundancy — the model must learn semantics to reconstruct masked regions. MAE pretraining is 3× faster than DINO and achieves excellent fine-tuning performance. [2]"
    },
    {
      "heading": "ViT Scaling: Variants and Parameter Counts",
      "body": "Accuracy vs efficiency: B/16 (86M, 17.6 GFLOPs) achieves 81.8% on ImageNet after fine-tuning from ImageNet-21k; L/16 (307M) achieves 85.2%. DINOv2 ViT-G/14 (1B params) achieves 86.5% without any supervised fine-tuning on ImageNet — just linear probing."
    }
  ],
  "takeaways": [
    "ViT splits image into P×P patches, flattens, linearly projects, prepends [CLS] token, adds positional embeddings, passes to Transformer encoder. [1]",
    "ViT-B/16: P=16, D=768, L=12 layers, 12 heads; 196 patches from 224×224 image + 1 CLS = 197 tokens. [1]",
    "No CNN inductive biases: ViT needs large datasets (ImageNet-21k or self-supervised) but models arbitrary long-range dependencies. [2]",
    "DINO ViT attention heads naturally segment foreground objects without supervision — emergent spatial structure. [2]",
    "MAE masks 75% of patches; reconstruction pretraining is 3× faster than contrastive methods and matches downstream accuracy. [2]"
  ],
  "edgeCases": [
    {
      "q": "Why does ViT use a [CLS] token for classification instead of average-pooling patch tokens?",
      "a": "Both approaches work in practice. The [CLS] token approach (from BERT) was used in the original ViT: the [CLS] token has no spatial meaning and is free to aggregate global information from all patch tokens via self-attention during training. Average pooling (GlobalAveragePool over all patch tokens) is conceptually simpler and was found to work equally well or slightly better in some ViT ablations. DeiT and many follow-up works use average pooling as the default. The [CLS] token has a practical advantage: it provides a fixed-size, position-independent global representation that's easy to use for retrieval and downstream tasks without knowing the sequence length. For dense prediction (segmentation, depth), patch tokens are used directly — the [CLS] token is discarded."
    },
    {
      "q": "How do you fine-tune a ViT pretrained on 224×224 images on a higher-resolution task?",
      "a": "ViT's positional embeddings are learned 1D vectors indexed by position — 196 position embeddings for 196 patches at 224×224. At 448×448 with P=16, you get 784 patches — more positions than the pretrained model has. Solution: 2D bicubic interpolation of the pretrained position embeddings from a 14×14 grid (196 positions) to a 28×28 grid (784 positions). PyTorch's interpolate function handles this: reshape the 196 embeddings to (14,14), interpolate to (28,28), flatten back to (784, D). This allows the model to generalise to unseen resolutions. The patch embedding (Conv2d weights) is reused directly — patch size P stays the same. Fine-tuning at higher resolution with interpolated PE typically gives 1–2% accuracy improvement over using the original 224×224 resolution."
    }
  ],
  "sources": [
    {
      "label": "Dosovitskiy et al. (2020) — An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale, ICLR 2021 arXiv:2010.11929",
      "url": "https://arxiv.org/abs/2010.11929"
    },
    {
      "label": "He et al. (2022) — Masked Autoencoders Are Scalable Vision Learners, CVPR 2022 arXiv:2111.06377",
      "url": "https://arxiv.org/abs/2111.06377"
    }
  ],
  "youtubeUrl": "https://www.youtube.com/embed/8phM16htKbU",
  "youtubeLabel": "Vit"
}
