{
  "eli3": [
    "Numerical stability refers to whether errors (rounding, measurement, truncation) grow or stay controlled during computation. A stable algorithm gives nearly the right answer to nearly the right question.",
    "Think of it like balancing a pole: unstable algorithms amplify small perturbations; stable ones dampen them. Gaussian elimination without pivoting can be unstable; with partial pivoting, it's usually stable.",
    "Conditioning is a property of the problem; stability is a property of the algorithm. A well-conditioned problem has solutions that change only slightly with input perturbations. An ill-conditioned problem amplifies errors regardless of algorithm."
  ],
  "whenWhy": [
    {
      "title": "Solving linear systems",
      "body": "Condition number κ(A) = ||A|| ||A⁻¹|| measures sensitivity. κ = 10⁶ means losing 6 digits of precision. Use QR or SVD for ill-conditioned systems instead of Gaussian elimination. Regularization (Ridge) improves conditioning."
    },
    {
      "title": "Neural network training",
      "body": "Vanishing/exploding gradients in deep networks (RNNs especially). Residual connections, batch normalization, careful initialization (Xavier, He) improve stability. Gradient clipping prevents explosion. Mixed precision requires careful scaling."
    },
    {
      "title": "Recurrence relations and ODEs",
      "body": "Some recurrences amplify errors exponentially. Computing e^x via Taylor series for large x is unstable; use exp(x) = exp(x/2)². ODE solvers choose step sizes to maintain stability (explicit vs implicit methods)."
    },
    {
      "title": "Statistical algorithms",
      "body": "Naive variance computation Σ(xᵢ - x̄)² can suffer catastrophic cancellation. Use Welford's online algorithm or two-pass method. Log-sum-exp for softmax. Log-determinants via Cholesky for positive definite matrices."
    }
  ],
  "deepDiveIntro": [
    "A problem is well-conditioned if small changes in input cause small changes in output. Condition number κ measures sensitivity: κ = sup ||Δoutput||/||output|| / ||Δinput||/||input||. Large κ means ill-conditioned. For Ax = b, κ(A) = ||A|| ||A⁻¹||. Solving with large κ loses log₁₀(κ) digits of precision.",
    "An algorithm is stable if it produces the exact solution to a slightly perturbed problem (backward stability). Stable algorithms can solve well-conditioned problems accurately. For ill-conditioned problems, even stable algorithms may produce inaccurate results — the problem itself is sensitive."
  ],
  "visualizationType": "numerical-stability",
  "deepDive": [
    {
      "heading": "Condition Number",
      "body": "The condition number measures how sensitive a problem is to tiny input errors. A small condition number means the problem itself is well behaved, while a large one means even perfect algorithms will struggle because the underlying task is fragile. This is why conditioning must be separated from the quality of the algorithm used to solve the problem.",
      "formula": "κ(A) = ||A|| ||A⁻¹|| = σₘₐₓ/σₘᵢₙ (spectral)"
    },
    {
      "heading": "Forward and Backward Stability",
      "body": "Forward stability asks whether the computed answer is directly close to the true answer. Backward stability asks a slightly different and more practical question: whether the algorithm produced the exact answer to a nearby problem. Backward stability is the standard benchmark because it combines nicely with conditioning to explain overall accuracy.",
      "formula": "Computed f(x) = f(x + δx) where ||δx||/||x|| is small"
    },
    {
      "heading": "Gaussian Elimination and Pivoting",
      "body": "Gaussian elimination can amplify rounding error if it eliminates using unlucky pivots. Pivoting reduces that risk by reordering rows, which is why partial pivoting is the standard practical choice in dense linear algebra. More aggressive pivoting can be safer in theory, but it costs more work.",
      "formula": "growth factor without pivoting can scale like 2ⁿ"
    },
    {
      "heading": "Avoiding Catastrophic Cancellation",
      "body": "Catastrophic cancellation happens when you subtract nearly equal numbers and destroy meaningful digits. A common fix is to rewrite the computation into an algebraically equivalent form that avoids this dangerous subtraction. Many stable numerical tricks, from log-sum-exp to improved quadratic-formula variants, follow exactly this pattern."
    },
    {
      "heading": "Vanishing and Exploding Gradients",
      "body": "Deep networks face the same stability issues in a different form: repeated transformations can either shrink gradients toward zero or blow them up. Architecture choices, initialization schemes, normalization, and clipping all try to keep signal and gradient magnitudes in a workable range. This is why numerical stability is not just a classical linear algebra topic but also a core machine learning concern.",
      "formula": "repeated products behave like Wⁿ"
    }
  ],
  "youtubeLabel": "Numerical Stability — MIT OCW",
  "youtubeUrl": "https://www.youtube.com/embed/7Po3Id-v57s",
  "takeaways": [
    "Condition number κ measures problem sensitivity (not algorithm property).",
    "Stability: does algorithm amplify errors? Backward stability is gold standard.",
    "Ill-conditioned problems amplify errors regardless of algorithm.",
    "Avoid catastrophic cancellation via algebraic reformulation.",
    "In ML: vanishing/exploding gradients controlled by architecture, initialization, normalization."
  ],
  "edgeCases": [
    {
      "q": "Can a stable algorithm give wrong answers on an ill-conditioned problem?",
      "a": "Yes, and this is expected. Backward stability means the answer is correct for slightly perturbed input. But for ill-conditioned problems, slight input perturbations cause large output changes. The problem is inherently sensitive, not the algorithm. Check condition number before blaming the algorithm."
    },
    {
      "q": "What's the difference between accuracy and stability?",
      "a": "Accuracy: how close computed is to true answer. Stability: does the algorithm control error growth. A stable algorithm can give inaccurate results on ill-conditioned problems. An unstable algorithm can give accurate results by luck on easy problems. Accuracy = stability + conditioning."
    },
    {
      "q": "Why does batch normalization help training stability?",
      "a": "Multiple effects: (1) Reduces internal covariate shift (distribution of layer inputs more stable). (2) Smooths optimization landscape (Lipschitz continuity). (3) Acts as implicit regularization. (4) Allows higher learning rates. (5) Makes gradients less sensitive to scale of parameters and initialization."
    }
  ],
  "sources": [
    { "label": "Trefethen & Bau — Numerical Linear Algebra", "url": "https://people.maths.ox.ac.uk/trefethen/text.html" },
    { "label": "Higham — Accuracy and Stability of Numerical Algorithms (2nd ed.)", "url": "https://www.maths.manchester.ac.uk/~higham/asna/" }
  ]
}
